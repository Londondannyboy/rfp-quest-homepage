{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "dev": {
      "cache": false,
      "persistent": true
    },
    "build": {
      "dependsOn": ["^build"],
      "outputs": [".next/**", "!.next/cache/**", "dist/**"],
      "env": ["NEXT_PUBLIC_NEON_AUTH_URL", "NEON_AUTH_BASE_URL", "NEON_AUTH_COOKIE_SECRET"]
    },
    "lint": {},
    "clean": {
      "cache": false
    }
  }
}
